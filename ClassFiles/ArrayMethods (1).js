const nums = [1, 2, 3, 4];
 // Map Function

 const doubled = nums.map(num => num * 2);

//  console.log(doubled);

 // find function

 const users = [
    {id: 1, names: "Ashish"},
    {id: 2, names: "Rahul"}
 ]

 const user = users.find(use => use.id === 2);
//  console.log(user);

 //ForEach 

 const variables = ["a", "b", "c"];

 variables.forEach(SingleVariable => {
    console.log(SingleVariable);
 })


 
